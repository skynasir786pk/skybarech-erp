window.SKYBARECH_CONFIG = Object.freeze({
  // Administrator setup only: put the same Hostinger API URL used by Android here
  // before building the Windows installer. Customers never see or enter this URL.
  apiBaseUrl: "https://lightgrey-cormorant-560478.hostingersite.com/api"
});
